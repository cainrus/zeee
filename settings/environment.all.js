module.exports = function(conf) {
/*    var environmentFile = '/home/dotcloud/environment.json';
    var environment = JSON.parse(require('fs').readFileSync(environmentFile));
    conf.socketport      = environment['PORT_SOCKETIO'];*/
};